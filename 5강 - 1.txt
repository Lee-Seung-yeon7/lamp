# 절댓값 구하기

#include <stdio.h>

int main (void)
{
int number;

printf ("숫자를 입력하세요 : ");
scanf ("%d", &number);

if (number < 0)        //number가 0보다 작으면
{ number = -number; }

printf("절댓값은 %d 입니다.\n", number);

getchar();
getchar();
getchar();
return 0;
}